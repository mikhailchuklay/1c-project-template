УстановитьПривилегированныйРежим(Истина); КлючЗадания = "block4_incoming_mail_purge"; Список = ФоновыеЗадания.ПолучитьФоновыеЗадания(Новый Структура("Ключ", КлючЗадания)); Стр = ""; Для Каждого З Из Список Цикл Стр = Стр + Строка(З.УникальныйИдентификатор) + "|state=" + Строка(З.Состояние) + "|start=" + Формат(З.Начало, "ДФ=yyyy-MM-dd HH:mm:ss") + "|end=" + Формат(З.Конец, "ДФ=yyyy-MM-dd HH:mm:ss") + ";"; КонецЦикла; З2 = Новый Запрос("ВЫБРАТЬ КОЛИЧЕСТВО(*) К ИЗ Документ.ЭлектронноеПисьмоВходящее"); Ост = З2.Выполнить().Выгрузить()[0].К; Результат = ?(ПустаяСтрока(Стр), "jobs=none", "jobs=" + Стр) + ";left=" + Ост;
