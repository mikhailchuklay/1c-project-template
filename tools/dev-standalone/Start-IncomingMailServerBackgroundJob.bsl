УстановитьПривилегированныйРежим(Истина); КлючЗадания = "block4_incoming_mail_purge"; ПутьКОбработке = "__EPF_PATH__"; Если ПустаяСтрока(ПутьКОбработке) Тогда Результат = "ERR: empty EPF path"; ИначеЕсли Не Новый Файл(ПутьКОбработке).Существует() Тогда Результат = "ERR: file not found: " + ПутьКОбработке; Иначе Активные = ФоновыеЗадания.ПолучитьФоновыеЗадания(Новый Структура("Ключ", КлючЗадания)); Для Каждого Задание Из Активные Цикл Если Задание.Состояние = СостояниеФоновогоЗадания.Активно Тогда Результат = "already_running=" + Строка(Задание.УникальныйИдентификатор); Прервать; КонецЕсли; КонецЦикла; Если Результат = Неопределено Тогда ИмяОбработки = ВнешниеОбработки.Подключить(ПутьКОбработке, Ложь); Параметры = Новый Массив; Параметры.Добавить(__BATCH__); Параметры.Добавить(2); Задание = ФоновыеЗадания.Выполнить(ИмяОбработки + ".УдалитьВсеВходящие", Параметры, КлючЗадания, "Блок4: удаление входящей почты"); Результат = "started=" + Строка(Задание.УникальныйИдентификатор) + ";method=" + ИмяОбработки; КонецЕсли; КонецЕсли;
