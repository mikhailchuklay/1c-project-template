УстановитьПривилегированныйРежим(Истина); Запрос = Новый Запрос("ВЫБРАТЬ ПЕРВЫЕ 1 Ссылка ИЗ Документ.ЭлектронноеПисьмоВходящее"); Ссылка = Запрос.Выполнить().Выгрузить()[0].Ссылка; Попытка ДокОбъект = Ссылка.ПолучитьОбъект(); ДокОбъект.Удалить(); Результат1 = "first=ok"; Исключение Результат1 = "first_err=" + ОписаниеОшибки(); КонецПопытки; Попытка ДокОбъект2 = Ссылка.ПолучитьОбъект(); Если ДокОбъект2 = Неопределено Тогда Результат2 = "second_obj=undefined"; Иначе ДокОбъект2.Удалить(); Результат2 = "second=ok"; КонецЕсли; Исключение Результат2 = "second_err=" + ОписаниеОшибки(); КонецПопытки; Результат = Результат1 + " || " + Результат2;
