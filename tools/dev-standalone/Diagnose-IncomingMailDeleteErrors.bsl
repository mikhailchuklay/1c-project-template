УстановитьПривилегированныйРежим(Истина); ОшибкиПоТексту = Новый Соответствие; Удалено = 0; Попыток = 0; Запрос = Новый Запрос("ВЫБРАТЬ ПЕРВЫЕ 30 Ссылка ИЗ Документ.ЭлектронноеПисьмоВходящее"); Выборка = Запрос.Выполнить().Выбрать(); Пока Выборка.Следующий() Цикл Попыток = Попыток + 1; Попытка ДокОбъект = Выборка.Ссылка.ПолучитьОбъект(); Если ДокОбъект = Неопределено Тогда ТекстОш = "ПолучитьОбъект=Неопределено"; Иначе ДокОбъект.Удалить(); Удалено = Удалено + 1; КонецЕсли; Исключение ТекстОш = СокрЛП(ОписаниеОшибки()); Если ПустаяСтрока(ТекстОш) Тогда ТекстОш = "пустое описание"; КонецЕсли; Кол = ОшибкиПоТексту.Получить(ТекстОш); Если Кол = Неопределено Тогда Кол = 0; КонецЕсли; ОшибкиПоТексту.Вставить(ТекстОш, Кол + 1); КонецПопытки; КонецЦикла; Строки = Новый Массив; Строки.Добавить("attempts=" + Попыток + ";deleted=" + Удалено + ";error_kinds=" + ОшибкиПоТексту.Количество()); Для Каждого КлючИЗначение Из ОшибкиПоТексту Цикл Строки.Добавить("[" + КлючИЗначение.Значение + "x] " + Лев(КлючИЗначение.Ключ, 500)); КонецЦикла; Результат = СтрСоединить(Строки, " || ");
