# Сторонние компоненты

Предназначен для хранения внешних зависимостей - библиотек, конфигураций и т.д.

- `mcp/` — дистрибутив **OneMCP** для `1c-data-mcp` (`OneMCP.cfe`, `ИнструментыДляРазработки.xml`). См. [mcp/README.md](mcp/README.md).
